nocturne flat scroll 2.2.0 - package verification
checked september 23, 2026 on windows 11, using windows powershell 5.1.

installer
33 checks ran against throwaway copies of the game files. the real game
folder was not used, and its files were unchanged afterwards.
- every package script parses in windows powershell 5.1.
- install previews (-whatif) leave the game folder unchanged for both
  loaders, and so does the uninstall preview.
- a game without melonloader gets the bundled bepinex and the plugin. a
  repeat install changes nothing.
- upgrading from the published 2.1.2 plugin installs 2.2.0 and keeps the
  old dll as a backup.
- with melonloader 0.7.3 installed and turned on, the mod goes into mods and
  the bepinex copy is disabled. a melonloader-only game gets no bepinex
  files. a repeat install changes nothing.
- after melonloader is removed, running the installer again brings back the
  bepinex plugin and disables the mods copy. only one copy is ever enabled.
- a melonloader that is turned off in userdata\loader.cfg, or whose proxy
  dll is missing, leaves bepinex in charge.
- a stray folder on the unused loader's path doesn't block the other loader.
- uninstall disables every enabled copy for both loaders, keeps the loaders,
  and repeats cleanly. declining its confirmation is reported as incomplete.
- the installer refuses these without changing any file: an unknown dll in
  either mod location, a corrupt payload, melonloader older than 0.7.3, a
  melonloader 0.5 layout, bepinex beside an enabled melonloader, melonloader
  when it isn't installed or won't start, a different steam build, an
  unknown game asset, a conflicting loader binary, and a disabled doorstop
  configuration. an existing compatible doorstop configuration is kept.

the real installer was also run on the test pc. without a folder argument
it found the steam installation and upgraded the installed 2.1.2 bepinex
plugin, keeping the old dll as a backup. it also installed the melonloader
mod into a separate copy of the game folder with melonloader 0.7.3. while
that copy was running, the running-game guard stopped an uninstall before
it changed anything.

the optional fullscreen script is unchanged since 2.1.2 and was not retested.

shareable dlls
- both are built from the 2.2.0 source with debug symbols disabled and no
  git commit hash, so they contain no development-machine paths.
- building the source folder with -c Release reproduces both dlls byte for
  byte, inside or outside a git checkout.

gameplay coverage
both loaders were tested in-game with qa builds of the same source, which
added logging and stepped through the settings. the options rows, their
limits, hold-to-repeat, reset to default, and the receptor positions in both
2d modes were checked; see technical-notes.md for the measurements. the
final dlls were then launched on both loaders and loaded without errors.
full-battle note hits, holds, animated meter effects, and a five-lane
visual test have not been verified.

see technical-notes.md for compatibility and third-party-notices.txt for
the official loader and corresponding-source provenance.
